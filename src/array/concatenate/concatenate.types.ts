// Public types for concatenate.
// Every exported name must start with "Concatenate".

/** Options for {@link concatenate}. */
export type ConcatenateOptions = {
  /** If true, the result holds each value one time only. Default: false. */
  readonly unique?: boolean
}
